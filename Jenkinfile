pipeline {
    agent {
        label 'Ansible'
    }
    stages {
        stage('Ec2 Creation') {
            steps {
                sh 'ansible-playbook -i inventory ec2.yml'
            }
        }
    }
}